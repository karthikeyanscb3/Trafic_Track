server.port=8081

# Database Configuration - Using environment variables for security
# Fallback to Neon connection if env vars not set (for local development only)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-raspy-butterfly-ahecpxtd-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_rhD1SKFXfPl0}
spring.datasource.driverClassName=org.postgresql.Driver

# Encryption Key for API Credentials
# REQUIRED: Set this environment variable before starting the application
# Generate a secure key: openssl rand -base64 32
# For local dev, you can use: APP_ENC_KEY=MySecureKey12345678901234567890123456789012
app.encryption.key=${APP_ENC_KEY:/O57NYDzbL6ML5Hinm/PrHEFjPjOSl9yxaQQ87rZ42k=}

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Optional: enable H2 console for local dev if still needed
spring.h2.console.enabled=false
